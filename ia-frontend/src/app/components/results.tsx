import React from 'react';

const Results: React.FC<{ predictions: number[] }> = ({ predictions }) => {
    return (
        <div className="container mx-auto">
            <h1 className="text-2xl font-bold mb-4">Results Prediction</h1>
            
        </div>
    );
};

export default Results;